# 📱 Frontend | EraseUnaVez 
Este repositorio es la app móvil inicial construida con Expo y TypeScript. Mantendremos el flujo de trabajo con Expo Go para desarrollo rápido en dispositivos.

## Requisitos previos
Tener instalado
* React Native (Framework principal)
* Expo Go (Entorno de desarrollo y ejecución)
* React Navigation (Navegación entre pantallas)

## Configuración e instalación 
1. Asegúrate de estar en la carpeta raíz del frontend (`ERASEUNAVEZ/FRONTEND/`).

2. Instala dependencias 
  Instala todas las dependencias de React Native
    ```
    npm install
    ```
3. Ejecuta la aplicación 
    ```
    npx expo start
    ```